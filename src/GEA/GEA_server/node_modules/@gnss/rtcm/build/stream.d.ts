/// <reference types="node" />
import stream = require('stream');
import { RtcmMessage } from './rtcm';
export declare class RtcmDecodeTransformStream extends stream.Transform {
    private readonly buffer;
    private index;
    private synchronized;
    private potential;
    private readonly closeOnError;
    constructor({ closeOnError, synchronizedInitially }?: {
        closeOnError?: boolean;
        synchronizedInitially?: boolean;
    });
    _transform(chunk: Buffer | string, encoding: string, callback: (error?: (Error | null), data?: any) => void): void;
}
export declare class RtcmEncodeTransformStream extends stream.Transform {
    private readonly buffer;
    constructor();
    _transform(chunk: RtcmMessage, encoding: string, callback: (error?: (Error | null), data?: any) => void): void;
}
